// Object
let person = {
    name: 'Sanket Lamsal',
    age : 19
}

// Dot Notation
console.log(person.age);// To access the element age

// Bracket Notation
let selection = 'name';
person[selection] = 'Changed';
console.log(person.name);