let first = 'Sanket';
let last = 'Lamsal';

const full = `${first} ${last}`;

// Starts with
console.log(full.startsWith('S'));

// Ends With
console.log(full.endsWith('L'));

// Includes
console.log(full.includes(' '));

// Repeat 
console.log(`${first} `.repeat(5));




